rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to add a lead (email subscription)
    match /leads/{leadId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
